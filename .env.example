# HelixForge Environment Variables
# Copy this file to .env and fill in the values.

# Required: OpenAI API key for LLM features (embeddings, semantic labeling)
OPENAI_API_KEY=sk-your-api-key-here

# Optional: Path to config file (default: config.yaml)
# HELIXFORGE_CONFIG=config.yaml

# Optional: Override any config.yaml setting via environment variables.
# Convention: HELIXFORGE_<SECTION>__<KEY>=value (double underscore for nesting)
#
# Examples:
# HELIXFORGE_LLM__PROVIDER=openai
# HELIXFORGE_LLM__MODEL=gpt-4o
# HELIXFORGE_API__PORT=8000
# HELIXFORGE_LOGGING__LEVEL=DEBUG
# HELIXFORGE_PROCESSING__MAX_FILE_SIZE_MB=1000
